# Experiment — 03_rounded_corner

Run script(s) for this study. See `../README.md` for the original-script mapping and
`docs/how_to_run_representative_simulations.md` for a representative run. Requires the
built C binary (`code/model/c_core/README.md`) and a precomputed floor field. The final
data behind the figures is already included in `results/03_rounded_corner/`, so re-running is optional.
