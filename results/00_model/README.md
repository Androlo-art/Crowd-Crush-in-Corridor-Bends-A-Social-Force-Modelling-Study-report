# results / 00_model

Supports the navigation floor-field figure (Ch2, `bend_floorfield`).

- `navigation_fields_d12/` — precomputed fast-marching navigation fields for the
  d = 1.2 m chamfered (r = 0.25) geometry, per turn angle.

Regenerate the figure: `code/plotting/plot_navigation_field.py` (see
`docs/how_to_regenerate_figures.md`). No simulation rerun needed.
